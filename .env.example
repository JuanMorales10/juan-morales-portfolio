# Dominio definitivo del portfolio. Se usa para las URL absolutas: canonical,
# Open Graph, sitemap y JSON-LD.
#
# Sin esta variable el sitio cae en un dominio provisional. Hay que definirla
# antes de publicar.
NEXT_PUBLIC_SITE_URL=https://tudominio.com
